# This file can update the JupyterHub Helm chart's default configuration values.
#
# For reference see the configuration reference and default values, but make
# sure to refer to the Helm chart version of interest to you!
#
# Introduction to YAML:     https://www.youtube.com/watch?v=cdLNKUoMc6c
# Chart config reference:   https://zero-to-jupyterhub.readthedocs.io/en/stable/resources/reference.html
# Chart default values:     https://github.com/jupyterhub/zero-to-jupyterhub-k8s/blob/HEAD/jupyterhub/values.yaml
# Available chart versions: https://hub.jupyter.org/helm-chart/
#

singleuser:
  defaultUrl: '/rstudio'
  image:
    name: andybayerroswell/naavre-rstudio
    tag: 20241114-1810
    pullPolicy: IfNotPresent
  cmd: null
  extraEnv:
    API_ENDPOINT:
      valueFrom:
        configMapKeyRef:
          name: configmap-naavre-rstudio
          key: API_ENDPOINT
    NAAVRE_API_TOKEN:
      valueFrom:
        secretKeyRef:
          name: secret-naavre-rstudio
          key: NAAVRE_API_TOKEN
  
# debug:
#   enabled: true

hub:
  config:
    KubeSpawner:
      # delete_grace_period: 300  # Time in seconds before forcefully killing the pod
      # delete_stopped_pods: false  # Prevents deletion of stopped pods
      container_security_context:
        privileged: true
        runAsUser: 0
        runAsGroup: 0
        allowPrivilegeEscalation: true

